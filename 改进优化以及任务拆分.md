# 改进优化以及任务拆分

## 0. 当前完成状态（2026-06-13）

### 0.1 总体结论

当前独立人脸核验服务的本地开发、自动化测试和生产部署已经完成，可通过
`https://faces.chbzg.com.cn` 访问。服务端已形成“模板 -> 业务绑定 session ->
连续帧综合核验 -> 一次性 proof -> 业务完成后 finalize”的生产接口链路。

当前尚不能判定整个互联网医院接入项目已完成，原因是 `chbzg` 业务系统尚未接入
上述 session/proof 链路，医生登录和审核写入口尚未完成服务端防绕过改造；真人、
非本人、照片、屏幕回放、视频回放和低配电脑的现场样本矩阵也尚未填写。

### 0.2 已完成

| 范围 | 状态 | 已验证内容 |
| --- | --- | --- |
| 动作活体 | 已完成 | 服务端随机生成 1-3 个动作，每动作采集 2 秒，MediaPipe 服务端判定 |
| 防翻拍 | 已完成 | 双 MiniFASNet ONNX 模型、抽样推理、有效帧数和通过比例门槛 |
| 人脸比对 | 已完成 | InsightFace/ArcFace、多帧 1:1 比对、最低分和通过比例联合判断 |
| 帧协议防护 | 已完成 | 帧数、顺序、动作、时间跨度、帧间隔、严格 Base64、重复帧和多脸拒绝 |
| 图像质量门禁 | 已完成 | 登记照和活体抽样帧先检查模糊、亮度、对比度、人脸占比和人脸框可见性 |
| 低配策略 | 已完成 | 360px、JPEG 0.7、200ms 采样、重模型只抽样 6 帧、前端无实时重模型 |
| 模板存储与管理 | 已完成（单实例） | SQLite 持久化模板、版本递增、新模板激活时旧模板自动吊销；支持查询版本和撤销模板 |
| 生产核验 API | 已完成 | 人员、场景、业务事件、记录和动作绑定；短期上传令牌；统一结果码 |
| 一次性 proof | 已完成 | proof 绑定业务事件，有 TTL，可 introspect，finalize 后不可再次作为有效凭证 |
| 失败明细持久化 | 已完成 | 核验会话持久化 `action_results`，可追踪动作、质量、PAD、人脸一致性和比对失败原因 |
| 服务保护 | 已完成（基础） | 内部 API Key、严格 CORS、请求字段/大小限制、模型异常与用户失败区分 |
| 就绪与模型 | 已完成 | `/api/health`、`/api/ready`、模型文件离线部署、生产实例模型预热 |
| 自动化测试 | 已完成 | 2026-06-18 本地 44 项 Python 测试、Python 语法、JS 语法和前端静态检查通过 |
| 生产部署 | 已完成 | systemd 已启用，监听 `127.0.0.1:8005`，Nginx HTTPS 域名反向代理 |
| 生产健康检查 | 已完成 | 2026-06-13 `health`、`ready` 均为 HTTP 200，模型无缺失，服务重启次数为 0 |

当前生产入口：`https://faces.chbzg.com.cn`

### 0.3 部分完成

| 范围 | 当前情况 | 仍需补齐 |
| --- | --- | --- |
| 高可用存储 | SQLite 已保证单机重启后模板、session、proof 不丢失 | 多实例部署前需要共享数据库/Redis、分布式锁和一致性验证 |
| 并发与稳定性 | session 状态更新具备原子条件，业务请求 ID 支持幂等 | 尚无明确的全局并发上限、排队、限流和压力测试结论 |
| 可观测性 | systemd/Nginx 日志可用，接口返回机器结果码 | 尚无结构化审计日志、耗时指标、告警和模型/阈值版本指标 |
| 模板生命周期 | 支持创建、版本递增、自动吊销旧模板、查询版本和撤销模板 | 尚无批量迁移、批量补录和失败重采管理工具 |
| 模型合规 | 模型已离线部署并可正常推理 | InsightFace 与 PAD 模型的生产使用和再分发许可仍需业务方确认 |

### 0.4 未完成

以下工作不属于独立识别算法代码的缺失，但属于“完整接入 chbzg 并正式验收”的
上线阻塞项：

1. `chbzg` 尚未实现 `FaceVerifyPolicy`、`FaceVerifyClient`、
   `FaceVerifyEventService` 和 `FaceVerifyGuard`。
2. 医生登录、医生审核、药师审核的最终后端写入口尚未强制校验并消费 proof。
3. `chbzg` 旧单图比对、前端动作判定和旧远程人脸接口尚未完成灰度替换及下线。
4. 现有医生/药师头像尚未批量转换为新版模板，失败名单和重新采集入口尚未建立。
5. 尚未用真实摄像头填写本人、非本人、照片、屏幕照片、视频回放和低配电脑验收数据。
6. 尚未执行面向业务流的禁用 JavaScript、直连接口、跨人员、跨场景、重放 proof
   等防绕过验收。
7. 尚未执行并发、网络波动、进程重启、多实例一致性、灰度和回滚演练。

### 0.5 下一步顺序

1. 先按 `人脸验证验收记录.md` 完成独立服务真实样本验收和最小幅度调参。
2. 在 `chbzg` 建立业务事件与 proof 消费表，接入登录和审核最终写入口。
3. 批量生成新版模板，失败人员进入人工重采，不复用旧模型向量。
4. 仅对指定医生进行灰度，完成防绕过、网络异常和回滚演练。
5. 灰度指标达标后再扩大人员范围并下线旧链路。

## 1. 目标与边界

本项目目标是完成人脸识别与活体检测模块：在医生登录等业务场景调用时，系统能够对基准人脸与摄像头实时人脸进行比对，并叠加静态人脸一致性与活体检测，尽量做到本人稳定通过，非本人、照片、屏幕翻拍、普通视频回放稳定不通过。

本轮只关注人脸识别功能本身，不关注 auth、账号、会话、权限、医院平台登录态等体系。难度控制为中等，优先修正当前 Demo 中影响验收的明显偏差；不做高成本模型训练、硬件级摄像头可信校验、深度伪造红队防护、账号数据库重构。

性能目标是低配电脑不卡顿。前端采集低分辨率帧，后端只抽样少量帧做重模型推理，动作数量控制在 1-3 个，每个动作采集 2 秒，避免实时逐帧跑 InsightFace、MediaPipe、ONNX 模型。

## 2. 项目基础与历史不足

项目以 FastAPI + 静态页面起步。演示流程为：上传基准照 -> 生成随机动作 ->
前端摄像头采帧 -> 后端动作活体检测 -> MiniFASNet 防翻拍 -> 活体帧与基准人脸
比对。当前已在此基础上增加持久化模板、业务绑定 session 和一次性 proof 接口；
`/api/compare` 仅保留 Demo 兼容用途，生产业务应使用 `/v1` 链路。

已具备的基础能力：

- 注册阶段使用 InsightFace/ArcFace 提取人脸特征，并拒绝上传图片中的多张人脸。
- 活体挑战支持眨眼、张嘴、摇头、点头、微笑等随机动作。
- 后端用 MediaPipe FaceMesh 判断动作是否完成。
- 后端使用 MiniFASNet ONNX 模型做被动防翻拍检测。
- 活体阶段会把摄像头人脸与基准人脸做一致性校验。
- challenge 有过期时间，且一次挑战只能验证一次。

初始版本不足及当前处理结果：

| 初始不足 | 当前处理结果 |
| --- | --- |
| 模板、challenge 和最终结果绑定不严格 | 已增加 enrollment 绑定；生产 `/v1` 进一步绑定人员、场景和业务事件 |
| Base64 帧不能证明一定来自真实摄像头 | 已增加时间、顺序、重复帧和随机动作校验；高级虚拟摄像头/注入仍属于能力边界 |
| README 阈值与运行配置容易漂移 | 已统一服务端配置，客户端不能提交认证阈值 |
| 采集时间短、低质量帧参与判断 | 已固定每动作 2 秒，并增加有效人脸比例和多帧一致性门槛 |
| 缺少帧协议与异常输入校验 | 已增加帧数、顺序、时间跨度、间隔、重复帧和严格 Base64 校验 |
| `/api/compare` 允许客户端传 threshold | 已禁止额外字段并固定服务端阈值；生产业务改用 `/v1` 综合核验 |
| 摄像头 UX 和错误处理偏弱 | 已增加就绪判断、关闭摄像头、上传预校验、超时和错误分类；实时质量提示未实现 |

仍然存在的能力边界：

- 浏览器提交 Base64 连续帧，无法从根本上证明视频源不是虚拟摄像头或高级注入。
- 当前生产部署为单实例 SQLite 架构，不等同于多实例高可用。
- 真实攻击样本效果必须以现场验收记录为准，自动化测试不能证明实际 FAR、FRR 或 IAPAR。

## 3. 成熟方案可吸取点

参考方向不是照搬某个 SDK，而是吸取成熟生物识别/PAD 系统的共性做法。

参考资料：

- NIST FATE PAD：评估软件型人脸 Presentation Attack Detection 算法在不同攻击介质上的表现，强调用攻击样本量化检测能力。https://pages.nist.gov/frvt/html/frvt_pad.html
- NISTIR 8491：Face Analysis Technology Evaluation (FATE) Part 10: Performance of Passive, Software-based PAD Algorithms。https://doi.org/10.6028/NIST.IR.8491
- ISO/IEC 30107-3:2023：规定 PAD 测试与报告原则，强调性能评估和攻击类型分类。https://www.iso.org/standard/79520.html
- FIDO Biometrics Requirements：用 FAR、FRR、IAPAR 等指标衡量生物识别和攻击呈现接受风险。https://fidoalliance.org/specs/biometric/requirements/Biometrics-Requirements-v4.0-fd-20240522.html

可吸取点：

- 模板先固定，再做活体：基准人脸模板、活体 challenge、最终比对结果必须属于同一轮验证。
- 静态比对和活体检测分层：静态相似度负责“是不是这个人”，活体/PAD 负责“是不是现场真人”。
- 主动活体与被动 PAD 组合：随机动作降低照片和普通视频复用风险，被动 PAD 识别屏幕/打印/翻拍纹理。
- 轻量质量门禁：先判断人脸是否入框、是否单人、帧数是否足够、时间跨度是否合理，再进入模型推理。
- 阈值服务端固定：认证阈值只允许后端配置，不让客户端传入影响最终判定。
- 指标化验收：用本人样本、非本人样本、照片样本、屏幕视频样本分别记录通过率/失败原因，而不是只看单次演示。
- 失败原因可操作：给使用者展示“光线不足、请靠近、动作幅度不足、不是本人”等简洁原因，内部日志保留分数细节。

## 4. 总体优化方案

### 4.0 本轮执行口径

为避免范围过大，本轮建议按以下口径直接执行：

- 保留“上传基准照/模板登记”的 Demo 入口，专注把人脸识别、活体检测、防翻拍链路做好。
- 不做 auth、账号系统、会话、权限、数据库重构，不做强虚拟摄像头识别。
- 后端以“动作活体 + 防翻拍 + 人脸一致性 + 帧协议校验”为主线加固。
- 前端以“稳定采集、不卡顿、错误清晰”为主线优化。
- 默认难度为中等：本人体验优先，同时拦截常见非本人、静态照片、屏幕照片、普通视频回放。
- 单次完整检测目标耗时为 6-10 秒，不做实时重模型推理。

### 4.1 中等难度版本的通过策略

推荐通过条件：

```text
基准人脸模板已确定
AND challenge 未过期且未使用
AND 提交帧动作顺序正确
AND 每个动作帧数量与时间跨度达标
AND 摄像头画面多数帧存在单张人脸
AND 随机动作全部通过
AND 防翻拍模型通过
AND 活体帧与基准模板相似度通过
```

为缩短流程并降低本人误拒，默认随机要求 1-3 个动作；当只生成 1 个动作时，仍必须同时通过防翻拍、人脸一致性和帧连续性检测。默认阈值不追求极限拦截率，先保证清晰摄像头、普通室内光线下本人能稳定通过。

建议首版默认参数：

| 参数 | 建议值 | 原因 |
| --- | --- | --- |
| 随机动作数量 | 1-3 个 | 缩短检测时间，安全性由动作、PAD 和人脸比对共同保障 |
| 每动作采集时长 | 2 秒 | 给普通用户足够完成动作的时间 |
| 采集间隔 | 200ms | 每动作约 11 帧，覆盖完整 2 秒，低配压力可控 |
| 上传帧宽度 | 360px | 兼顾识别与网络/CPU 压力 |
| JPEG 质量 | 0.7 | 控制请求体大小 |
| InsightFace 抽样 | 6-8 帧 | 避免全部帧跑重模型 |
| 防翻拍抽样 | 6 帧 | 保持中等强度，避免低配卡顿 |
| challenge TTL | 120-180 秒 | 足够操作，同时降低重放窗口 |

### 4.2 低配电脑性能策略

- 前端采集分辨率控制在 360px 宽左右，JPEG 质量 0.65-0.75。
- 每个动作采集 2 秒，间隔约 200ms，约 11 帧，提交帧覆盖完整 2 秒。
- 后端动作检测可以使用该动作的全部轻量帧，但 InsightFace 和 anti-spoofing 只抽样 6-8 帧。
- 防翻拍 ONNX 模型不做实时推理，只在提交后批量抽样推理。
- 不增加复杂实时滤镜、WebGL、前端模型推理，避免低配电脑卡顿。
- 前端检测中禁用重复点击，避免同时发起多次重模型请求。

### 4.3 模板与挑战绑定策略

本轮不做 auth 体系，只做人脸模板与活体挑战的绑定：

- 上传基准照后生成 `enrollment_id`，活体 challenge 必须绑定该 `enrollment_id`。
- `/api/liveness/verify` 只接受本 challenge 对应的 `enrollment_id`，避免提交时串换模板。
- `/api/compare` 只使用服务端 challenge 中已通过活体的人脸结果，不允许客户端修改阈值。
- 同一 challenge 只能验证一次，过期或失败后必须重新生成。
- 业务系统未来只需要提供基准模板来源；本项目只保证模板比对与活体判定能力。

### 4.4 活体与防翻拍策略

保留当前“随机动作 + MiniFASNet 防翻拍 + 人脸一致性”的组合，但做轻量增强：

- challenge 使用更安全的随机源生成动作。
- 默认动作数量为 1-3；单动作场景仍保留 PAD、人脸一致性和重复帧检测。
- 每帧提交 `action`、`index`、`timestamp`，服务端校验顺序、数量、时间跨度。
- 服务端限制总帧数、单帧大小、总请求体大小，避免资源消耗。
- 增加重复帧/低变化检测，拦截简单脚本重复提交同一张图。
- 活体阶段若检测到多张人脸，直接失败，避免旁人干扰。
- `/api/compare` 不再接受客户端 threshold，最终认证只认服务端配置。

### 4.5 前端体验策略

- 摄像头开启前检测浏览器能力，不支持时给出明确提示。
- 等待 `loadedmetadata/canplay` 后再允许生成动作和开始检测。
- 增加关闭摄像头按钮，检测结束后可释放摄像头。
- 上传前检查图片类型和大小。
- 请求增加 timeout，避免卡住。
- 错误文案分类：摄像头权限、未检测到人脸、动作幅度不足、不是本人、防翻拍失败。
- 页面显示简洁结果，详细分数只放在动作明细或后端日志，避免使用者根据分数反复调参攻击。

## 5. 验收矩阵

最终验收建议按下表记录，每个用例至少保留最终结果、失败原因、总耗时、是否卡顿。

| 场景 | 样本 | 预期结果 | 优先失败原因 |
| --- | --- | --- | --- |
| 本人正常验证 | 本人、普通室内光线、正对摄像头 | 应通过 | 如失败，应提示动作幅度不足、人脸不清晰或光线问题 |
| 本人弱光/偏远 | 本人、光线较弱或距离偏远 | 可失败但原因要清晰 | 未检测到稳定人脸、质量不足 |
| 非本人 | 其他人使用同一基准模板 | 必须失败 | 人脸一致性失败 |
| 静态照片 | 纸质照片或手机展示照片 | 必须失败 | 动作失败或防翻拍失败 |
| 屏幕照片 | 电脑/手机屏幕展示医生照片 | 必须失败 | 防翻拍失败或动作失败 |
| 普通视频回放 | 手机/屏幕播放医生自拍视频 | 应大部分失败 | 动作顺序/时间跨度、防翻拍或人脸一致性失败 |
| 重复帧脚本 | 同一帧重复提交 | 必须失败 | 重复帧或时间跨度校验失败 |
| 低配电脑 | 低性能电脑浏览器完成流程 | 页面不卡死 | 若失败，应是检测失败而不是浏览器无响应 |

首版验收目标：

- 本人正常场景 5 次中至少 4 次通过。
- 非本人、静态照片、屏幕照片、重复帧脚本全部失败。
- 普通视频回放原则上失败；若个别通过，记录样本并作为下一轮阈值/动作组合调优依据。
- 单次检测总耗时控制在 6-10 秒。
- 检测期间按钮状态明确，页面没有明显卡死。

## 6. 推荐任务拆分

### 阶段一：文档与验收口径确认

目标：先确认实现范围，避免后续过严或过重。

任务：

- 固化本文件中的目标、边界和性能约束。
- 默认验收样本：本人正常摄像头、非本人、本人照片、屏幕展示照片、普通视频回放、重复帧脚本、低配电脑。
- 默认本轮采用“模板登记/演示模式”，不做 auth、账号、会话、权限。
- 默认动作数量为 1-3，低配和速度优先。

验收标准：

- 文档确认后再开始代码实现。
- 不把本轮目标描述为“绝对防所有攻击”，而是“中等难度下明显优于当前 Demo”。

### 阶段二：后端轻量加固

目标：提升认证链路可信度和稳定性，不显著增加计算压力。

任务：

- 增加帧协议字段：`index`、`timestamp`。
- 增加帧数量、动作顺序、时间跨度、重复帧校验。
- 调整默认动作数量为 1-3。
- 调整抽样参数：InsightFace/PAD 抽样 6-8 帧。
- 统一服务端阈值，移除或忽略客户端 threshold。
- Base64 解码改为严格模式，并限制单帧大小。
- 模型加载/推理异常做兜底，返回用户可理解的失败原因。
- 活体阶段尽量拒绝多脸画面。

建议优先级：

- P0：帧数量/顺序/时间跨度、服务端阈值固定、默认动作数量。
- P1：重复帧检测、严格 Base64、异常兜底。
- P2：多脸拒绝、详细调试日志。

### 阶段三：前端采集与 UX 优化

目标：让本人更容易通过，让失败原因更清晰，同时保持低配不卡顿。

任务：

- 摄像头就绪后再允许操作。
- 采集帧携带 `index`、`timestamp`。
- 每动作采集 2 秒，每 200ms 左右一帧。
- 检测中禁用重复点击。
- 增加关闭摄像头按钮。
- 增加图片上传类型/大小校验。
- 请求增加超时和非 JSON 响应保护。
- 页面文案改为人脸验证语境。

建议优先级：

- P0：采集元数据、摄像头就绪判断、检测中防重复。
- P1：错误分类、上传预校验、请求超时。
- P2：关闭摄像头、设备选择、实时质量提示。

### 阶段四：人脸验证链路收敛

目标：把 Demo 收敛成可被业务系统调用的人脸验证模块。

任务：

- 保留上传基准照作为模板登记入口。
- challenge 严格绑定 `enrollment_id`，不允许跨模板复用。
- 最终结果由 `/api/liveness/verify` 给出综合判定，`/api/compare` 仅作为兼容接口。
- 返回结果区分活体失败、防翻拍失败、人脸不一致、帧协议失败。
- 保留必要调试明细，方便验收时定位误拒或误受原因。

建议优先级：

- P0：challenge 绑定基准模板。
- P1：结果分类与调试明细。
- P2：模板删除、更新、过期策略。

### 阶段五：验收测试与调参

目标：用固定样本验证“本人通过、非本人/照片/视频不过”的实际效果。

测试集建议：

- 本人正常：同一医生在室内普通光线下测试 5 次。
- 非本人：不同人员用同一基准模板测试 3-5 次。
- 照片攻击：纸质照片或手机展示静态照片测试 3-5 次。
- 视频攻击：手机/屏幕播放本人自拍视频测试 3-5 次。
- 低配测试：在低性能电脑或浏览器降频环境下测试是否卡顿。

通过标准建议：

- 本人正常通过率不低于 80%，失败原因应可操作。
- 非本人应全部失败，失败类型优先为人脸一致性失败。
- 静态照片应全部失败，失败类型优先为动作或防翻拍失败。
- 普通屏幕视频应大部分失败；若个别通过，需要记录样本并微调防翻拍阈值或动作组合。
- 单次检测总耗时建议控制在 6-10 秒内。
- 低配设备检测期间页面不明显卡死，按钮状态清晰。

调参原则：

- 本人失败过多：优先放宽动作幅度阈值或延长采集时长，不优先降低人脸一致性底线。
- 照片通过：优先增加动作组合稳定性和重复帧校验，再微调防翻拍阈值。
- 视频通过：优先检查时间跨度、动作随机性、防翻拍抽样帧，再考虑增加一个动作。
- 低配卡顿：优先降低抽样帧数和上传宽度，不增加前端实时模型。

## 7. 本轮建议实现顺序

建议按以下顺序实现，风险最低：

1. 后端帧协议校验和默认参数调整。
2. 前端采集元数据、采集时长、摄像头就绪判断。
3. 服务端阈值固定和异常兜底。
4. 收敛验证结果与失败原因。
5. 验收样本测试并根据失败原因微调阈值。

## 8. 暂不建议本轮实现的内容

以下内容成本高，且容易造成低配卡顿或工期扩张，本轮不建议做：

- 自训练或重训练活体检测模型。
- 前端实时逐帧跑人脸识别或活体模型。
- 虚拟摄像头/视频注入的强检测。
- 3D 深度摄像头、红外、硬件安全模块。
- auth、账号、会话、权限、医院平台登录态。
- 极严格阈值导致医生频繁误拒。

## 9. 默认拍板项

如无额外要求，后续实现默认按以下拍板项执行：

- 本轮不做 auth、账号、会话、权限，保留模板登记/演示模式。
- 验收覆盖本人、非本人、静态照片、屏幕照片、普通视频回放、重复帧脚本和低配设备体验。
- 难度为中等，本人体验优先，同时拦截常见照片和普通视频。
- 默认动作数量 1-3，每动作 2 秒，单次检测约 2-8 秒，低配不卡顿优先。
- 代码实现必须先满足 P0，再做 P1；P2 作为有余量时补强。

## 10. 文件级实现清单

后续进入代码实现时，建议只改以下文件，避免无关重构。

| 文件 | 改动重点 | 优先级 |
| --- | --- | --- |
| `backend/app/config.py` | 调整默认动作数量、采样帧数、帧数量/时间跨度/重复帧阈值、单帧大小限制 | P0 |
| `backend/app/schemas.py` | 给 `FramePayload` 增加 `index`、`timestamp` 字段，限制列表长度和字符串长度 | P0 |
| `backend/app/main.py` | 增加帧协议校验、重复帧校验、服务端阈值固定、异常兜底、响应失败原因收敛 | P0 |
| `backend/app/liveness.py` | 使用更安全的随机动作生成方式，保证默认动作数量为 1-3 | P0 |
| `backend/app/face_engine.py` | 严格 Base64 解码，避免非法输入和超大输入造成资源浪费 | P1 |
| `backend/app/anti_spoofing.py` | 控制防翻拍抽样帧数，推理异常返回可控失败结果 | P1 |
| `backend/static/app.js` | 摄像头就绪判断、采集元数据、请求超时、上传预校验、检测中防重复、低配采集参数 | P0 |
| `backend/static/index.html` | 人脸验证语境、关闭摄像头按钮、状态区域文案 | P1 |
| `backend/static/styles.css` | 按新增状态/按钮补最小样式，保持轻量 | P1 |
| `README.md` | 同步运行说明、默认参数、验收步骤和局限性 | P1 |
| `tests/` | 新增轻量单元测试，覆盖帧协议、阈值固定、重复帧、Base64 输入 | P1 |

不建议改动：

- 不新增大型前端框架。
- 不引入需要编译的大型依赖。
- 不重写 InsightFace、MediaPipe、MiniFASNet 的核心推理逻辑。
- 不把内存 Demo 强行改成完整 auth 或数据库系统。

## 11. 测试命令清单

后续实现完成后，建议按以下顺序验证。

环境与语法：

```bash
cd face-verify-demo
PYTHONPYCACHEPREFIX=/private/tmp/face-verify-demo-pycache .venv/bin/python -m py_compile backend/app/*.py
```

单元测试：

```bash
cd face-verify-demo
PYTHONPYCACHEPREFIX=/private/tmp/face-verify-demo-pycache .venv/bin/python -m unittest discover -s tests -v
```

接口启动：

```bash
cd face-verify-demo
.venv/bin/uvicorn backend.app.main:app --host 127.0.0.1 --port 8000
```

健康检查：

```bash
curl -s http://127.0.0.1:8000/api/health
```

浏览器验收：

- 打开 `http://127.0.0.1:8000`。
- 上传本人清晰正脸作为模板。
- 开启摄像头，生成动作，完成检测。
- 按 `人脸验证验收记录.md` 记录本人、非本人、照片、屏幕照片、视频回放、重复帧脚本、低配体验的结果。

低配体验观察项：

- 点击“开始检测”后页面是否还能响应。
- 视频画面是否持续流畅。
- 按钮是否正确禁用和恢复。
- 单次检测是否大致落在 6-10 秒。
- 失败原因是否可理解，而不是只显示技术异常。

## 12. 最终完成判定

只有同时满足以下条件，才认为本项目达成本轮目标：

- 文档已说明当前能力、成熟方案借鉴点、优化方案、任务拆分、验收矩阵。
- 后端已完成 P0 加固：帧协议、时间跨度、重复帧、服务端阈值、默认动作数量。
- 前端已完成 P0 优化：摄像头就绪、采集元数据、检测防重复、低配采集参数。
- 已运行语法检查和单元测试。
- 已启动服务并通过健康检查。
- 已用浏览器打开页面确认主流程入口正常。
- 对无法自动验证的真人/照片/视频样本，已给出明确人工验收步骤和调参原则。
- 现场验收完成前，不应宣称“本人稳定通过、非本人/照片/视频稳定失败”已经被真实样本证明。
